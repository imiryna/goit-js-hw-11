const { default: axios } = require('axios');
import { Notify } from 'notiflix/build/notiflix-notify-aio';
export axios from 'axios';

// Notiflix.Notify.failure('Qui timide rogat docet negare');

// Notiflix.Notify.failure('Click Me', {
//   timeout: 3000,
// });
